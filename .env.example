# Copy to .env.local — Vite picks up VITE_ prefixed vars on the client.
# Server-only (Vercel functions) can read non-prefixed vars like GEMINI_API_KEY.

# Server-side (api/suggest.js)
GEMINI_API_KEY=

# Optional: model override
GEMINI_MODEL=gemini-1.5-flash
